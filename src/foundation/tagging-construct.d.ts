import { IConstruct } from 'constructs';
import { PlatformConfig } from '../interfaces/platform-config';
export declare class TaggingConstruct {
    static applyTags(resource: IConstruct, config: PlatformConfig): void;
}
